package inventory;

public class PlayerInventory extends inventory.Inventory {

    // Konstruktor: Initialisiert das Spielerinventar
    public PlayerInventory(String[] initialItems) {
        super(initialItems); // Ruft den Konstruktor der Basisklasse auf
    }

    // Spieler-spezifische Methoden können hier ergänzt werden (falls erforderlich)
}
