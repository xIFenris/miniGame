package inventory;

public class MonsterInventory extends Inventory {

    // Konstruktor: Initialisiert das Monsterinventar
    public MonsterInventory(String[] initialItems) {
        super(initialItems); // Ruft den Konstruktor der Basisklasse auf
    }

    // Monster-spezifische Logiken können hier ergänzt werden (falls nötig)
}
